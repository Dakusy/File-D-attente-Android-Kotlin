<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.file_attente">
    <!-- Adding Camera, Write External Storage Permission -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:configChanges="locale"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar"
        tools:replace="android:theme">
        <activity android:name=".languages.Settings"></activity>
        <activity android:name=".Login" />
        <activity android:name=".Guest" />
        <activity android:name=".Show_Picture_Conseil" />
        <activity android:name=".Picture_Conseil" />
        <activity android:name=".Show_Ordo_Nom" />
        <activity android:name=".Show_Conseil_Nom" />
        <activity android:name=".Nom_Ordonnance" />
        <activity android:name=".Nom_Conseil" />
        <activity android:name=".Conseil" />
        <activity android:name=".Ordonnance" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>